import type { AtlasStrategy } from './types';

// Preset strategies array is kept empty by design to allow full user customization and clean state
export const ATLAS_PRESET_STRATEGIES: AtlasStrategy[] = [];

